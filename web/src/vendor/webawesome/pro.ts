/**
 * Web Awesome Pro is loaded through the owner's purchased project module.
 * This public repository never contains project URLs, license keys, or Pro assets.
 * Domain components detect Pro custom elements and progressively enhance their
 * semantic HTML fallbacks when those elements are registered.
 */
export const proComponentTags = ['wa-line-chart'] as const;
