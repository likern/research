name: Check research workspace

on:
  push:
    branches:
      - main
    paths:
      - ".nushell-version"
      - ".typst-version"
      - "research.nu"
      - "research.toml"
      - "README.md"
      - "design/diagrams/**"
      - "docs/nushell-workspace.md"
      - "docs/semantic-diagrams.md"
      - "ydmp/**/*.typ"
      - "ydmp/**/*.bib"
      - "ydmp/**/paper.toml"
      - "ydmp/**/prepare.json"
      - ".github/workflows/check-research.yml"
  pull_request:
    paths:
      - ".nushell-version"
      - ".typst-version"
      - "research.nu"
      - "research.toml"
      - "README.md"
      - "design/diagrams/**"
      - "docs/nushell-workspace.md"
      - "docs/semantic-diagrams.md"
      - "ydmp/**/*.typ"
      - "ydmp/**/*.bib"
      - "ydmp/**/paper.toml"
      - "ydmp/**/prepare.json"
      - ".github/workflows/check-research.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Read pinned versions
        id: versions
        shell: bash
        run: |
          echo "nushell=$(tr -d '[:space:]' < .nushell-version)" >> "$GITHUB_OUTPUT"
          echo "typst=$(tr -d '[:space:]' < .typst-version)" >> "$GITHUB_OUTPUT"

      - name: Install pinned Nushell
        uses: hustcer/setup-nu@f3fd65374ffc4d60974c0dd2f7263c6c5c285f81 # v3
        with:
          version: ${{ steps.versions.outputs.nushell }}

      - name: Install pinned Typst
        uses: typst-community/setup-typst@40d32bb6ccb235bd7f5164cb47cfd40a5ea3a314 # v5
        with:
          typst-version: ${{ steps.versions.outputs.typst }}

      - name: Check toolchain
        run: nu research.nu doctor

      - name: Smoke-test module interface
        run: nu -c 'use ./research.nu; research list papers | length | print'

      - name: Compile every registered document
        run: nu research.nu check
